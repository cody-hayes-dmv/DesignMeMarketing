{
  "name": "yourseodashboard-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:marketing\" \"npm run dev:app\" \"npm run dev:server\"",
    "dev:marketing": "cd marketing && npm run dev",
    "dev:app": "cd app && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:marketing && npm run build:app",
    "build:marketing": "cd marketing && npm run build",
    "build:app": "cd app && npm run build",
    "install:all": "npm install && cd marketing && npm install && cd ../app && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.13.1"
  }
}
